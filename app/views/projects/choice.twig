{% block body %}
    <div class="listProjet">
        <div class="notBloc">
            <p class="dateNot">Votre Freelance</p>

            <img src="{{ home ~ propositions.0.account.picture }}" height="66px" width="66px" alt="Notification user"/>

            <p>
                <a href="{{ home }}users/profile/{{ propositions.0.account.id }}">{{ propositions.0.account.firstname ~ " " ~ propositions.0.account.lastname }}</a>
                travail sur votre projet
            </p>

            <hr class="clear"/>

        </div>
        <a href="{{ home }}projects/end/{{ project.id }}" class="buttonWhite saveInfo">Cloturer ce projet</a>

    </div>

    <hr class="clear"/>

{% endblock %}

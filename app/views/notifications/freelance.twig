{% block body %}
    {% for date, participation in participations %}
        <div class="notBloc">
            <p class="dateNot">{{ participation.0.created_at | date("d F Y") }}</p>
            <ul>
                {% for notification in participation %}
                    <li>
                        <img src="{{ home ~ notification.account.picture }}" alt="Notification user"/>

                        <p>
                            Votre demande de participation concernant le projet <a
                                    href="{{ home }}projects/{{ notification.project.id }}">{{ notification.project.name }}</a>
                            a été {{ translateStatus(notification.status) }} par <a
                                    href="{{ home }}users/profile/{{ notification.account.id }}">{{ notification.account.firstname ~ " " ~ notification.account.lastname }}</a>
                        </p>

                        <hr class="clear"/>
                    </li>
                {% endfor %}

            </ul>
        </div>
    {% endfor %}
{% endblock %}